<UserControl x:Class="TypeConverters.CustomControls.UserProfileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TypeConverters.CustomControls"
             mc:Ignorable="d" 
             x:Name="UserProfile"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="UserName"/>
        <Label Grid.Column="1" Grid.Row="0" Content="{Binding CurrentUser.UserName, ElementName=UserProfile}"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Age"/>
        <Label Grid.Column="1" Grid.Row="1" Content="{Binding CurrentUser.Age, ElementName=UserProfile}"/>
    </Grid>
</UserControl>
